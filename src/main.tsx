import React from 'react';
import ReactDOM from 'react-dom/client';
import GameView from './GameView';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')!).render(<GameView />);
